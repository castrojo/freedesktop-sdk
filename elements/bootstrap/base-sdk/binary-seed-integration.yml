# SPDX-FileCopyrightText: Freedesktop-SDK Developers
# SPDX-License-Identifier: MIT

public:
  bst:
    integration-commands:
    - |
      ldconfig

    - |
      mkdir -p "%{indep-libdir}/locale"
      localedef --no-archive -i en_US -f UTF-8 en_US.UTF-8
