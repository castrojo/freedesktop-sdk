diff --git a/setup.py b/setup.py
index 6ff80ee32b..e6d8d8440a 100644
--- a/setup.py
+++ b/setup.py
@@ -507,6 +507,13 @@ def cython_extensions_and_necessity():
     else:
         core_c_files = list(CORE_C_FILES)
         extra_objects = []
+
+    absl_vendored_sources = (
+        []
+        if BUILD_WITH_SYSTEM_ABSL
+        else ["third_party/abseil-cpp/absl/log/initialize.cc"]
+    )
+
     extensions = [
         Extension(
             name=module_name,
@@ -515,7 +522,7 @@ def cython_extensions_and_necessity():
                 + list(CYTHON_HELPER_C_FILES)
                 + core_c_files
                 + asm_files
-                + ["third_party/abseil-cpp/absl/log/initialize.cc"]
+                + absl_vendored_sources
             ),
             include_dirs=list(EXTENSION_INCLUDE_DIRECTORIES),
             libraries=list(EXTENSION_LIBRARIES),
