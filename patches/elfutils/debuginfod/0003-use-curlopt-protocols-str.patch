From c915858bec79d7ff7988b44c46b32bd7d97866ad Mon Sep 17 00:00:00 2001
From: bbhtt <bbhtt.zn0i8@slmail.me>
Date: Mon, 22 Jan 2024 00:27:33 +0530
Subject: [PATCH] Cherry pick 6560fb26a62ef135a804357ef4f15a47de3e49b3 on top
 of 0.187

---
 debuginfod/debuginfod-client.c | 5 +++++
 1 file changed, 5 insertions(+)

diff --git a/debuginfod/debuginfod-client.c b/debuginfod/debuginfod-client.c
index ea6e461a..d6e98f31 100644
--- a/debuginfod/debuginfod-client.c
+++ b/debuginfod/debuginfod-client.c
@@ -1017,8 +1017,13 @@ debuginfod_query_server (debuginfod_client *c,
 
       /* Only allow http:// + https:// + file:// so we aren't being
 	 redirected to some unsupported protocol.  */
+#if CURL_AT_LEAST_VERSION(7, 85, 0)
+      curl_easy_setopt(data[i].handle, CURLOPT_PROTOCOLS_STR,
+                       "http,https,file");
+#else
       curl_easy_setopt(data[i].handle, CURLOPT_PROTOCOLS,
 		       CURLPROTO_HTTP | CURLPROTO_HTTPS | CURLPROTO_FILE);
+#endif
       curl_easy_setopt(data[i].handle, CURLOPT_URL, data[i].url);
       if (vfd >= 0)
 	curl_easy_setopt(data[i].handle, CURLOPT_ERRORBUFFER, data[i].errbuf);
-- 
2.43.0

